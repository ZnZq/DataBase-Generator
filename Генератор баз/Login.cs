namespace Генератор_баз
{
    public class Login
    {
        public string login = "";
        public string base_domain = "";
        public Login(string login, string domain)
        {
            this.login = login;
            base_domain = domain;
        }
    }
}